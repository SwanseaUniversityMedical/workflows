name: Flake8-nb Action

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - '.github/workflows/workflows-release-flake8-nb-action.yaml'
      - '.github/actions/flake8-nb/**'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/workflows-release-flake8-nb-action.yaml'
      - '.github/actions/flake8-nb/**'

permissions:
  actions: read
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Cancel early on pull requests if new commits are added
  # Don't cancel on release pushes
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  flake8-action:
    uses: SwanseaUniversityMedical/workflows/.github/workflows/pr-and-release-repo.yaml@v2.1.2-repo
    with:
      job-name: flake8-nb-action
      comment-release: true
      release-tag-format: 'v${version}-flake8-nb-action'
