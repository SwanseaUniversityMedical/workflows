name: Generic Sonar PR Decoration Action

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - '.github/workflows/workflows-release-generic-sonar-pr-decoration-action.yaml'
      - '.github/actions/generic-sonar-pr-decoration/**'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/workflows-release-generic-sonar-pr-decoration-action.yaml'
      - '.github/actions/generic-sonar-pr-decoration/**'

permissions:
  actions: read
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Cancel early on pull requests if new commits are added
  # Don't cancel on release pushes
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  generic-sonar-pr-decoration-action:
    uses: SwanseaUniversityMedical/workflows/.github/workflows/pr-and-release-repo.yaml@v2.1.2-repo
    with:
      job-name: generic-sonar-pr-decoration-action
      comment-release: true
      release-tag-format: 'v${version}-generic-sonar-pr-decoration-action'
